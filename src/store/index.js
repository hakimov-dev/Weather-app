import { createStore } from 'vuex'
import axios from 'axios'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
    async getIpAddress () {
      try {

      } catch (e) {
        console.log(e)
      }
    }
  },
  modules: {
  }
})
